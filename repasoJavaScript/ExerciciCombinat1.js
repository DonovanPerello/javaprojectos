/*Crea un array de números, després utilitza un bucle for per calcular la suma de tots els números a l'array
i mostra el resultat.*/

let numero = [1,2,3];
let contador = 0;
let suma=0;
for (let i = 0; i < numero.length; i++){

    contador = contador +numero[i];
}

console.log(contador)


